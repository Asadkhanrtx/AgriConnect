apiVersion: v1
kind: Service
metadata:
  name: media-service
  labels:
    app: media-service
spec:
  type: ClusterIP
  selector:
    app: media-service
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
