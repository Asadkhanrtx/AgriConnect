# Dev environment — delegates to root module
# Run: cd terraform/ && terraform apply -var-file=terraform.tfvars
# This file exists as a placeholder if you want to use workspaces or
# a separate state backend per environment in the future.

# For single-environment deployments, run Terraform from terraform/ directly.
